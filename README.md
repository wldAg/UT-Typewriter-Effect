# UT-Typewriter-Effect / UT打字机字幕

一个基于 UT 打字机实现的字幕程序

## 功能特点

- 实现屏幕上UT打字机逐字显示效果
- 支持字幕速度调节，修改颜色等

## 使用方法
你需要exe程序和Raylib.dll(自带)，并且自行创建Font文件夹(自带)和Run.txt文件(可以改名字，默认Run.txt)
程序是从外部文件读取命令(类似于脚本)的
按F3+ESC可以中途退出程序(全局监听,注意快捷键冲突)
### 脚本格式(参数必须对其且不能多不能少--就是开发时偷了个懒)
```
命令格式(注意命令格式，本程序对格式极为敏感，请一定遵守命令格式，没有文件内注释功能):
    第一行必须为set命令,格式:
        set 作者(指创建这个文件的人)屏幕宽度 作者屏幕高度 时间模式 启动键
        屏幕分辨率为为适应不同分辨率屏幕而设计的
        时间模式:
            timeline/tl:已时间刻为一条线，到了时间就执行
            timeout/to:已时间为延迟，过了延迟才执行
        启动键:
            用来启动时时停的(用来点击某个按键开始视频或其它东西时同步)，默认开启提示
            格式:字符/\转义字符[|]
                转义字符:
                    b       空格
                    W       左Win
                    w       右Win
                    A       左Alt
                    a       右Alt
                    S       左Shift
                    s       右Shift
                    E       ESC键
                    !~+     F1~F12
                如果启动键结尾为|则不显示提示文字，如:\b| 按空格键开始，但屏幕上没有提示文字
    文本显示:
        开始/延迟时间(以刻为单位，程序已60Tick/s速度运行) X坐标,Y坐标 文本(空格使用\b(不是\\b)代替) 字体(放在Font文件夹中一定要和字体文件名一样) 字体大小
        开始/延迟时间 X坐标,Y坐标 文本 字体 字体大小
    清屏/结尾延迟退出:
        开始时间(Tick) [特效]
        特效:
            outbreak/ob     文字破碎下落
            以后再更新吧...

\转义命令(注意大小写):
    \为转义符
    \\    打印一个\
    \n      换行
    \x      回车
    \y      单纯换行

    设置字体为
    \D      黑色
    \W      白色
    \R      红色
    \G      绿色
    \B      蓝色
    \P      紫色
    \C      青色
    \O      橙色
    \Y      黄色
    
    注意:以下命令为特效命令，需要成对出现或最终用\r还原：\1abcd\1或者\1abcd\r，否则会干扰后面不需要特效的文字
    \1      文字震动
    \2      渐变色
    \3      微震(随机字符轻微抖动)
    \4      转圈
    \5      上下震动
    \r      重置特效和颜色(指后面的，如有不懂，自行参考
                    case 'r':
                        Shake = false;
                        ChangeColor = false;
                        SmallShake = false;
                        LRShake = false;
                        UpDown = false;
                        color = Color.White;
                        break;)

    时间延迟(指文本中单个字符修改延迟):
    \t      完整格式\t时间|（不要忘记结尾的|）,延迟刻
    \T      完整格式\T时间|（不要忘记结尾的|）,修改主延迟

/转义符(同样注意大小写):
    /B      
    /B+颜色字符:
        D   黑色
        W   白色
        R   红色
        G   绿色
        B   蓝色
        C   青色
        P   紫色
        O   橙色
    (小写为半透明,这里就不写了)
```
```
示例文本Run.txt:
set 1920 1080 to \b
300 400,50 \T3|/B\5Hello\b\5\t40|我是\2Ag大爹\2\Y开发\W的\T30|字幕菌 text 80
120 ob
120 700,880 \T3|\1我可以震动\1 text 80
80 ob
120 700,880 \T3|\2可以变色\2 text 80
80 ob
120 700,800 \T3|\3偷偷震动\3 text 80
90 ob
120 700,800 \T3|\4转圈圈ing...\4 text 80
90 ob
120 700,800 \T3|\5上下浮动\5 text 80
90 ob
120 700,800 \T3|\R以\Y及\B变\P色\C哦\G。 text 80
90 ob
120 100,100 \T3|还可以自定义位置 text 80
110 ob
360 400,600 \T2|换行\n\t60|任意换行?\y\t60|123456\n\T40|A\xg\x大\x爹\T2|\n重叠字符 text 60
100 ob
360 400,300 \T3|\1\R叠\G加\C特\1\W\3\P效也\3\W\5\Y"可以"\5\2\4的哦\4\5...\5\n\W\1\2\3\4\5(部分特效间不兼容)\r text 80
120 ob
400 150,200 \T8|/B\Ybey\R=（\n\W对了，对Win7有点不支持呵呵\nF3+ESC可以提前退出\n(我去怎么不早说) text 120
120 ob
```

#### 命令行启动
- 如果你使用的启动文件为Run.txt，则直接运行exe启动
- 如果使用的其它文件名(text.txt,Main,Main.run,...)则需要使用 程序 [文件名] 来启动程序，不然会报错或打开默认的Run.txt文件
- 可以通过命令行输入 程序 (cf/cratefile/help)命令查看示例程序

#### 直接启动
准备一个Run.txt文件，然后启动程序，默认有示例文件

## 开发方法

1. 下载本项目代码
2. 还需要Visual Studio(本人用的2026),.Net 10
3. 用Visual Studio打开 基于UT文本引擎的字幕 by 无聊的Ag.slnx 文件，或直接双击 基于UT文本引擎的字幕 by 无聊的Ag.slnx 文件
4. 你需要通过nuget包管理来安装Raylib-cs(或者已经安装了)
5. 编译可以使用Visual Studio中的debug或Release编译，或者使用我提供的AOT编译中的AOT.bat编译(注意：AOT编译时间(20\~30s)>>>Visual Studio的简单编译(0.8\~1.5s))

## 运行环境

- Windows 7+
- 目前对Win7有点bug，导致程序启动时显示黑色背景，有时点击一下屏幕就会显示，目前不清楚原因
